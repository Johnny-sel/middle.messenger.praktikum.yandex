import {div, span, a, article, aside, button, component} from '../';
import {header, img, input, label, li, main, nav, p, section, ul} from '../';
import {footer, form, h1, h2, h3, h4, h5, h6} from '../';

describe('tags', () => {
  test('div tag to be definded', () => {
    expect(div).toBeDefined();
  });
  test('span tag to be definded', () => {
    expect(span).toBeDefined();
  });
  test('a tag to be defined', () => {
    expect(a).toBeDefined();
  });
  test('article tag to be defined', () => {
    expect(article).toBeDefined();
  });
  test('aside tag to be defined', () => {
    expect(aside).toBeDefined();
  });
  test('button tag to be defined', () => {
    expect(button).toBeDefined();
  });
  test('component tag to be defined', () => {
    expect(component).toBeDefined();
  });
  test('footer tag to be defined', () => {
    expect(footer).toBeDefined();
  });
  test('form tag to be defined', () => {
    expect(form).toBeDefined();
  });
  test('h1 tag to be defined', () => {
    expect(h1).toBeDefined();
  });
  test('h2 tag to be defined', () => {
    expect(h2).toBeDefined();
  });
  test('h3 tag to be defined', () => {
    expect(h3).toBeDefined();
  });
  test('h4 tag to be defined', () => {
    expect(h4).toBeDefined();
  });
  test('h5 tag to be defined', () => {
    expect(h5).toBeDefined();
  });
  test('h6 tag to be defined', () => {
    expect(h6).toBeDefined();
  });
  test('header tag to be defined', () => {
    expect(header).toBeDefined();
  });
  test('img tag to be defined', () => {
    expect(img).toBeDefined();
  });
  test('input tag to be defined', () => {
    expect(input).toBeDefined();
  });
  test('label tag to be defined', () => {
    expect(label).toBeDefined();
  });
  test('li tag to be defined', () => {
    expect(li).toBeDefined();
  });
  test('main tag to be defined', () => {
    expect(main).toBeDefined();
  });
  test('nav tag to be defined', () => {
    expect(nav).toBeDefined();
  });
  test('p tag to be defined', () => {
    expect(p).toBeDefined();
  });
  test('section tag to be defined', () => {
    expect(section).toBeDefined();
  });
  test('ul tag to be defined', () => {
    expect(ul).toBeDefined();
  });
});
