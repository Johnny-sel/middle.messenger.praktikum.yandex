export { isArr, isStr, isNum, isFunc, isObject } from './types';
export { first, last, penultimate, lastIndex, isDiffLength } from './array';
export { isEmpty } from './object';
export { random } from './math';
export { isHTMLElement } from './dom';
