{
  "author": "Evgenii Seleznev",
  "name": "messenger",
  "version": "1.0.0",
  "description": "messenger yandex practicum",
  "main": "index.js",

  "engines": {
    "node": ">= 18.12.1"
  },

  "scripts": {
    "clean-dist": "rm -rf dist && mkdir dist",
    "init": "npm run clean-dist",
    "dev": "npm run init && parcel ./src/index.hbs",
    "build": "npm run init && parcel build ./src/index.hbs"
  },


  "license": "ISC",
  "browserslist": "> 0.5%, last 2 versions, not dead",

  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.2",
    "parcel": "^2.8.2",
    "parcel-transformer-handlebars": "^1.0.0"
  }
}